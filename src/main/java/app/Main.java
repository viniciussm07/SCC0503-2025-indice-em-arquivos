package app;

public class Main {
    public static void main(String[] args) {
        // Para rodar CLI:
        new app.cli.CLI().run();
        // No futuro, para rodar GUI:
        // new app.gui.MainWindow().show();
    }
} 